# MedASR Server Configuration

server:
  host: "0.0.0.0"
  port: 8000

model:
  name: "google/medasr"
  chunk_length_s: 20
  stride_length_s: 2
  device: "auto"  # "auto", "cuda", or "cpu"

huggingface:
  token: null  # Optional: HF token for private models or rate limit increase
